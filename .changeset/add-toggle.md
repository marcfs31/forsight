---
"@marcfs31/fors-observability-design-system": minor
---

Add `Toggle` — a single pressed/unpressed control (`@radix-ui/react-toggle`) for a standalone toolbar action ("show grid lines", "pin sidebar"), styled as a button rather than `Switch`'s track-and-thumb. For a set of related toggles, use `ToggleGroup` instead.
