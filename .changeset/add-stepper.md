---
"@marcfs31/forsight": minor
---

Add `Stepper` — a multi-step wizard/setup-progress indicator (done/current/upcoming). A real `<ol>` so a screen reader announces the step count and position, with the current step marked via `aria-current="step"`. For a bounded progress percentage instead of discrete named steps, use `Progress`.
