---
"@marcfs31/fors-observability-design-system": minor
---

Add `Kbd` — a single keyboard key/shortcut token rendered as a native `<kbd>`, for use inside `CommandItem`, `Tooltip`, or anywhere a shortcut hint is shown. Compose several side by side for a multi-key shortcut (e.g. `⌘` + `K`).
