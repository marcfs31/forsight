---
"@marcfs31/fors-observability-design-system": minor
---

Add `EmptyState` — placeholder content for a table, list, or search result with nothing to show (icon, title, description, action slot). Renders `role="status"` by default so a screen reader user is told the section resolved to empty; override via the `role` prop for a static (non-dynamic) empty section.
