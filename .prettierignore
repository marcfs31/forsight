dist
storybook-static
node_modules
coverage
package-lock.json
CHANGELOG.md
fixtures/**/.next
fixtures/**/next-env.d.ts

# graft's generated agent wiring — regenerated by `graft init` / `graft upgrade`,
# so formatting it ourselves would just go red again after the next upgrade.
.claude/helpers
.claude/settings.json
.claude/skills/graft
.mcp.json

# botobs is a separate Go module with its own toolchain and conventions
# (gofmt for Go source; its own README/docs style) — not part of this
# project's formatting gate. See eslint.config.js's matching exclusion.
botobs
