{
  "name": "botobs-web",
  "private": true,
  "type": "module",
  "description": "botobs's dashboard — a small React app built on @marcfs31/fors-observability-design-system, the same design system this repo publishes. Built once via `vite build`; its static output is embedded into the Go binary (see internal/api/dashboard_embed.go).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build"
  },
  "dependencies": {
    "@marcfs31/fors-observability-design-system": "file:../..",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4.3.1",
    "tailwindcss": "^4",
    "typescript": "^5.5.4",
    "vite": "^6.4.3"
  }
}
