{
  "name": "forsight-web",
  "private": true,
  "type": "module",
  "description": "forsight's dashboard \u2014 a small React app built on @marcfs31/forsight, the same design system this repo publishes. Built once via `vite build`; its static output is embedded into the Go binary (see internal/api/dashboard_embed.go).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build"
  },
  "dependencies": {
    "@marcfs31/forsight": "^3.0.0",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4.3.1",
    "tailwindcss": "^4",
    "typescript": "^5.5.4",
    "vite": "^6.4.3"
  }
}
